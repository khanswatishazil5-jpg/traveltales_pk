/// A single saved image/trail shown inside the Premium Vault
/// (the screen the user sees after logging in).
class GalleryAsset {
  final String image;
  final String name;

  const GalleryAsset({
    required this.image,
    required this.name,
  });
}
