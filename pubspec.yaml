name: travel_tales
description: "Travel Tales — Instagram-style app for travel destinations."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2

  google_maps_flutter: ^2.10.0
  http: ^1.2.2
  shared_preferences: ^2.3.3
  crypto: ^3.0.5
  image_picker: ^1.1.2
  google_fonts: ^6.2.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
